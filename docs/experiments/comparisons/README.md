# Experiment Comparisons

Use this directory for cross-run comparison tables, ablation summaries, and
promotion/stop decisions that depend on multiple run records.

Single-run records should go under:

```text
docs/experiments/runs/
```

